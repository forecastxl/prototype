import middleware from './middleware'

describe('error middleware', () => {
  it('should catch any errors that are not submission errors', () => {})

  it('should ignore submission errors', () => {})
})
